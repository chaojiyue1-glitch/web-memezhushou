# Codex 开发规则

## 必读顺序

1. `docs/superpowers/specs/2026-09-04-meme-radar-design.md`
2. 当前被用户批准执行的 `docs/superpowers/plans/` 计划
3. `docs/ACCEPTANCE_TESTS.md`

## 阶段闸门

- 未经用户明确批准，不得开始任何功能开发。
- 一次只执行一份计划。完成后必须停止并提交测试结果、已知限制和用户验证步骤。
- 用户未明确批准前，不得进入下一阶段，不得自行扩展功能。
- 当前计划未覆盖的需求先记录并询问，不得顺手实现。

## 强制边界

- 永不连接钱包、读取助记词/私钥、签名或执行交易。
- 不输出“建议买入”、收益承诺或安全保证。
- 不实现云端部署、多用户、手机端、AI判断、自动交易、仓位盈亏或复杂刷量图谱。
- 第一版数据源仅限 Robinhood Chain、Pons、Uniswap、Blockscout、配置的数据市场接口与 X 官方 API。
- X 总开关关闭时禁止任何 X 网络请求；测试也不得消耗真实 X 额度。
- 关机、停止或暂停期间不补采历史数据。

## 工程规则

- 使用 Python 3.12、FastAPI、SQLite、SQLAlchemy 2.x、httpx、Web3.py、APScheduler、Jinja2。
- 采用测试驱动：先写失败测试，再实现最小代码，再运行测试。
- 外部 API 必须通过适配器访问；页面与评分模块不得直接调用第三方 API。
- 所有合约地址集中配置，并在实现前从官方来源再次核验。
- 不把 API 密钥、`.env`、数据库、日志或缓存提交到 Git。
- 日志必须脱敏授权头和密钥。
- 每个任务单独提交，提交信息清楚说明范围。
- 完成声明前运行计划指定的测试、`pytest`、`ruff check .` 和 `git diff --check`。

## 与用户沟通

- 使用简洁中文，避免要求用户理解代码。
- 每阶段提供可直接双击或复制执行的验证步骤。
- 数据缺失必须显示“未知/数据不足”，不得用 0 或“安全”代替。

